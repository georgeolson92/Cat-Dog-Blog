<div class="detail">
  <img src={{post.image}} alt={{post.title}}>
  <h2>by {{post.author.fullName}} | {{post.date}}</h2>
  <p>
    {{post.content}}
  </p>
  <button {{action 'delete' post}} class="btn btn-danger">Delete Post</button>
</div>
